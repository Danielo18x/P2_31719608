import {Router} from 'express';
import {ContactsController} from '../controler/controler';

const router = Router();

//router.get('/contacts', ContactsController.access);
//CREO QUE HAY QUE BORRAR EL ARCHIVO

export default router;